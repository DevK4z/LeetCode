#include <bits/stdc++.h>
using namespace std;
class Solution {
public:
    // time complexity: O(n)
    // Built-in function approach
    int strStr(string haystack, string needle) {
        int res = haystack.find(needle);
        return res;
    }
};